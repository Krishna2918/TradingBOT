{
  "_description": "Feature Consistency System Configuration - Production Environment",
  "_version": "1.0",
  "_environment": "production",
  "_created": "2025-10-27T12:00:00.000000",
  
  "warmup_trim_days": {
    "value": 200,
    "description": "Number of days to trim from start to remove indicator warm-up period",
    "min": 0,
    "max": 500,
    "environment_note": "Standard production warmup period for stable indicators"
  },
  
  "nan_drop_threshold_per_symbol": {
    "value": 0.05,
    "description": "Maximum NaN ratio allowed per symbol (0.05 = 5%)",
    "min": 0.01,
    "max": 0.50,
    "environment_note": "Strict threshold for production data quality"
  },
  
  "global_feature_keep_ratio": {
    "value": 0.95,
    "description": "Minimum symbol coverage required to keep feature globally (0.95 = 95%)",
    "min": 0.50,
    "max": 1.00,
    "environment_note": "High threshold for production feature stability"
  },
  
  "min_symbol_feature_coverage": {
    "value": 0.90,
    "description": "Minimum feature coverage required to include symbol (0.90 = 90%)",
    "min": 0.50,
    "max": 1.00,
    "environment_note": "High coverage requirement for production quality"
  },
  
  "use_missingness_mask": {
    "value": true,
    "description": "Whether to create _isnan columns for missing value indicators",
    "environment_note": "Enabled for production model robustness"
  },
  
  "imputation_strategy": {
    "value": "zero",
    "description": "Strategy for final imputation: 'zero', 'mean', 'median'",
    "options": ["zero", "mean", "median"],
    "environment_note": "Zero imputation for production consistency"
  },
  
  "imputation_value": {
    "value": 0.0,
    "description": "Value to use for final imputation when strategy is 'zero'",
    "environment_note": "Standard zero fill for production"
  },
  
  "manifest_path": {
    "value": "models/feature_manifest.json",
    "description": "Path to save/load feature manifest JSON file",
    "environment_note": "Production manifest location"
  },
  
  "config_backup_path": {
    "value": "models/feature_consistency_config.json",
    "description": "Path to save configuration backups",
    "environment_note": "Production backup location"
  },
  
  "log_level": {
    "value": "INFO",
    "description": "Logging level (DEBUG, INFO, WARNING, ERROR)",
    "options": ["DEBUG", "INFO", "WARNING", "ERROR"],
    "environment_note": "Standard logging for production monitoring"
  },
  
  "detailed_logging": {
    "value": true,
    "description": "Enable detailed logging of processing decisions",
    "environment_note": "Detailed logging for production monitoring and debugging"
  },
  
  "performance_settings": {
    "description": "Production-specific performance settings",
    "batch_processing_enabled": true,
    "memory_optimization": true,
    "parallel_processing": false,
    "max_symbols_per_batch": 50,
    "environment_note": "Optimized for production performance and stability"
  },
  
  "monitoring_settings": {
    "description": "Production monitoring configuration",
    "enable_metrics_collection": true,
    "enable_drift_detection": true,
    "alert_on_high_exclusion_rate": true,
    "exclusion_rate_threshold": 0.20,
    "environment_note": "Enhanced monitoring for production deployment"
  },
  
  "validation_rules": {
    "description": "Configuration validation rules",
    "rules": {
      "warmup_trim_days": "Must be non-negative integer",
      "nan_drop_threshold_per_symbol": "Must be between 0.01 and 0.50",
      "global_feature_keep_ratio": "Must be between 0.50 and 1.00",
      "min_symbol_feature_coverage": "Must be between 0.50 and 1.00",
      "imputation_strategy": "Must be one of: zero, mean, median",
      "log_level": "Must be one of: DEBUG, INFO, WARNING, ERROR"
    }
  }
}